# 绘本开场

2026-09-06：已接入用户新提供的 `7506e0d350aa6cf460a53ca8cf47271a.mp4`。

启动项目（F5）先显示绘本封面，点击“开始阅读”播放视频，末尾经暖米白纸页淡入第一张故事图。三张图按用户上传顺序展示，暂不添加旁白。点击画面、右下角按钮，或按空格、Enter、右方向键翻页；第三张翻过后进入第一天。第一天的计时和操作在翻页结束后启用。视频中的“跳过开篇”也会进入第一张故事图。

## 项目内配置

在 Godot 文件系统中选择 `config/opening.tres`，检查器可修改视频、封面、音量、按钮文字和位置、纸页颜色及后续场景。

- 原素材为 960×720、4:3、24 fps、约 5.06 秒的 HEVC/AAC MP4。
- 游戏播放 `assets/opening/book_open.ogv`，保留完整画面及音轨，没有沿用旧视频的裁切和去黑帧处理。
- 封面 `assets/opening/book_cover.png` 来自转码后视频首帧，静态封面和视频使用相同显示区域。
- 保留 4:3 比例，两侧以暖米白补齐，避免拉宽绘本。
- `next_scene` 已指向 `scenes/opening/story_opening.tscn`；故事结束接原来的 `scenes/main.tscn`。
- `config/story.tres` 配置三张图片的顺序、纸色和目标游戏场景。图片保存在 `assets/story/`，保持原比例完整显示。
- 故事复用 `scripts/page_turn.gd`，翻页节奏、卷曲、阴影、纸纹沿用 `config/mvp.tres` 中的翻页参数。翻动期间锁住重复点击，在纸张完全遮挡时更换图片或准备第一天。
- 原游戏的主场景和玩法逻辑没有改动。单独打开 `scenes/main.tscn` 按 F6，仍可直接试玩第一天。

## 请用户确认

1. Godot 如提示文件被外部修改，选择“从磁盘重新加载”；等素材导入结束后按 F5（运行项目）。
2. 封面按钮位置是否合适，两侧纸色留白是否接受。
3. 点击后是否有明显位置跳动、黑闪或声音问题。
4. 视频结束能否依次看到三张故事图，第三次翻页能否进入第一天并正常收集、放置枝条。
5. 翻页中连续点击是否会跳过图片，第一天是否从完整的白天时间开始。

按用户要求，本次未做收尾运行或自查测试。
