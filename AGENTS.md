# 协作方式

- 提方案和建议时，优先分析预期效果、可行性及对既有内容的影响。
- 修改文件前先明确方向并向用户汇报，用户确认后再施工；以效率优先，尽量缩小改动范围，保留前面构筑。已经确认的范围内不重复询问。
- 用户要求：完成内容修改后直接汇报，不做收尾自查、自动化测试或主动运行游戏验收；除非用户另行要求测试，或更高优先级指令明确要求。
- 汇报应简短说明：改了什么、预期效果、需要用户亲自测试什么。
- 当前主入口是 `scenes/opening/book_opening.tscn`；开场参数在 `config/opening.tres`，原游戏场景仍是 `scenes/main.tscn`。
