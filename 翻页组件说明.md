# 绘本卷页

夜晚结束后使用约 1.55 秒的二维模拟卷页：右下角起纸、弯曲折线掠过、纸背遮挡、揭开清晨。带静态细纸纹、折痕暗部、弧面亮部和随抬纸程度变化的软阴影。

## 接入现状

- `scripts/page_turn.gd` 创建独立顶层 CanvasLayer，覆盖世界、托盘、时钟及按钮。
- `shaders/page_turn.gdshader` 绘制卷页、纸背和光影，没有新增外部素材依赖。
- `main.gd` 收到 `covered` 信号时完成枝条成熟、天数变化、资源刷新、伙伴离场准备，并直接准备好白天天空。
- 收到 `finished` 信号后进入 DAY，开始白天计时和交互。翻页期间不推进伙伴及资源逻辑，菜单输入也不开放。
- 白天到黄昏、黄昏到夜晚仍使用原有天空过渡。夜晚到清晨不再重复播放天空旋转。
- 保留原有单次翻页音效，白天音乐在落页后恢复。

## 在检查器中调节

打开 `config/mvp.tres`：

| 参数 | 当前值 | 用途 |
|---|---|---|
| Page Duration | 1.55 | 总体时长，另有等待绘制的少量帧时间 |
| Swap Progress | 0.48 | 覆盖与揭开的时间分配 |
| Page Paper Color | 暖米白 | 纸背颜色 |
| Page Curl Strength | 0.75 | 折线弯曲及纸背卷曲宽度 |
| Page Shadow Strength | 0.30 | 纸张投影深度 |
| Page Grain Strength | 0.012 | 纸纹强度 |

## 故事页复用方式

将 `scripts/page_turn.gd` 实例加入故事场景：

```gdscript
var turn = preload("res://scripts/page_turn.gd").new()
add_child(turn)
turn.covered.connect(show_preloaded_next_story_page)
turn.finished.connect(enable_reading_and_fade_in_narration)
turn.play(1.8)
```

提前加载下一页图片，`covered` 回调里同步替换图文；`finished` 后开放继续按钮、淡入旁白。故事场景也应在 `turn.active` 时阻止自己的键盘快捷操作。

## 由用户体验

此次未运行游戏或测试。原游戏场景是 `scenes/main.tscn`：打开后按 F6 可绕过开场纸页进入第一天。请观察夜晚到次日：卷页质感、落页速度、是否直接露出清晨、新枝条是否成熟，以及计时是否在落页后开始。

旧的测试脚本中通过直接反复调用 `main._process()` 推动翻页的方式，不能再单独完成这个依赖 Tween 和实际绘制帧的动画；本次未修改或执行这些测试。
