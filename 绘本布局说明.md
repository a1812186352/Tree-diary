# 统一绘本布局

## 两种跨页状态

- 所有运行场景使用 1920×1080 的统一设计坐标，窗口按比例缩放，默认 1280×720，最小 960×540。F11 在窗口与全屏间切换。非 16:9 窗口保留比例留边，不扩大逻辑画幅或拉伸主体。
- 书本外框固定在 (56,48)，尺寸 1808×984；内部可见区域为 (80,72)，尺寸 1760×936。外围、纸张边缘、阴影由全局 BookFrame 共用。
- 阅读跨页：左侧完整等比插画，右侧标题、正文、页码与按钮。故事和成功/失败结局使用同一个 ReadingSpread 组件。
- 游戏跨页：两页连通，沿用一台相机和原有世界坐标。中间书脊仅为淡淡的装饰，不影响吸附、收集、动物或枝条穿过中线。
- 书框是固定遮挡层，树、土地、动物和翻页动画到页边自然被遮住；在框外不能进行游戏世界操作。无需裁剪或改写原素材。
- 开场封面和视频不显示书框，共用窗口内的等比显示区域，保留完整 4:3 画面。第一张故事页的白色光幕退去后，书框用 0.3 秒淡入；后续书框保持固定。
- 故事和结局插画共用整个左页区域 (80,72,880,936)，等比居中尽量放大。方形插画显示为 880×880，上下各保留约 28 像素纸色，不裁剪主体；其他比例按左页范围等比适配。

## 文字配置

- `config/story.tres`：`page_narrations` 已填入三张故事的用户文案；`page_titles` 留空，不再显示“序章”标题。
- `config/mvp.tres` 的“结局插画”：两种结局各有图片、标题与正文。现有结局短句作为默认文案。
- 故事与结局只显示正文，按作者填写的换行排列，不自动折行。右页文字整体居中、每行居中，行距为 72 个设计像素；优先楷体，回退微软雅黑，默认字号 36。超长行按该页共同字号缩小，以保留整句一行。
- 故事正文在白幕退去或翻页结束后用 0.45 秒淡入；结局正文直接淡入。没有逐字播放，也不要求等待淡入后才能继续。
- 透明花草框 `assets/ui/reading_floral_frame.png` 使用用户抠好背景的原图，铺在故事与结局插画上方、文字和按钮下方，不拦截鼠标。游戏和视频不添加此装饰。
- 修改统一几何位置时使用 `scripts/book_layout.gd`；书框绘制为 `scripts/book_frame.gd`，图文组件为 `scripts/reading_spread.gd`。

## 游戏界面

- 托盘与枝条卡片上移到书页底部，爱心位于左侧托盘上方；时钟和菜单收进右上页内。
- 游戏页顶部的操作提示与状态小字已移除；结局隐藏游戏界面，右页保留“再种一棵”，重开直接回第一天。一次性教学待后续制作。
- 世界相机的默认视角、缩放范围和坐标转换继续沿用，避免改变树根位置与接枝手感。

## 请用户试玩

1. F5 从封面开始，分别正常播完和跳过视频，观察封面、三张故事图、游戏和两种结局的外框是否固定。
2. 切换 F11、缩放窗口：插画是否完整，文字、按钮是否等比缩放且不被页边遮住。
3. 游戏中缩放、拖动镜头，在书页中线附近收集、接枝；将鼠标拖出页边后松开，确认不误放枝条。
4. 查看底部三个卡片是否完整可点、资源与爱心是否清楚、右上时钟和菜单是否正常。
5. 在故事和结局配置中填写一段较长正文，确认换行、滚动与翻页/重开按钮互不遮挡。

按用户协作原则，本轮完成后未运行游戏、自动化测试或收尾自查，效果由用户试玩确认。
