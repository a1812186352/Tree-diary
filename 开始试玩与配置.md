# 向光生长 MVP 试玩与配置

打开 `project.godot`，按 F5 试玩。目标平台为 PC，设计画布 1920×1080，初始窗口 1280×720，等比缩放。

## 试玩

- 白天点击阳光和水滴收集到口袋。
- 按住托盘里的单芽、双芽或三芽枝条，拖到树上的空闲芽点，松手放置。
- 拖动时用滚轮或 Q / E 调整朝向，F 镜像，右键或 Esc 取消。
- 新枝条当天保留纸边，新芽点要等翻页后才开放。未使用芽点跨日保留。
- 树从初始三芽节点开始。每个芽点最多连接一根子枝，单芽之后仍然可以接双芽、三芽。
- 前三层以临时概率产生橡果，第四层起产生羽毛。预兆出现在父连接芽点和新枝尾端芽点之间，第二天黄昏才对应一只伙伴。
- 地面伙伴处理地面虫；小鸟处理飞虫。三个鸟图各自代表一个独立伙伴，不作为动画帧轮播。
- 六天结束时仍有爱心即胜利。树底座、三种枝条和白天／黄昏／夜晚天空已接入提供的图片，虫和预兆暂时使用占位表现。

## 手动拖动坐标

打开 `scenes/main.tscn`：

- `World/Anchors/GroundAnchors`：地面伙伴站位。
- `World/Anchors/BirdAnchors`：鸟类备用站位，正常优先使用已成长枝条的停枝点。
- `World/Anchors/PestGroundAnchor`：地面虫目标。
- `World/Anchors/PestAirAnchor`：暂无已成长枝条时的空中目标。
- `World/Anchors/GroundEntryLeft/Right`、`BirdEntryLeft/Right`：伙伴和害虫入场点。
- `World/Anchors/ResourceSpawns`：资源出现位置。
- `World/Anchors/HeartAnchor`：爱心起点。
- `UI/TraySlots`、`ResourcePocketAnchor`、`ClockAnchor`：托盘、口袋、时钟位置。

这些都是可在 2D 编辑器中直接拖动的 Marker2D 空对象，不需要改坐标代码。

## 枝条模板和替换贴图

打开 `scenes/branches/base.tscn`、`one_bud.tscn`、`two_bud.tscn`、`three_bud.tscn`：

- 根节点的 `Sticker Texture` / `Grown Texture` 接受拖入贴图；`Texture Rect` 控制绘制范围。
- `RootAnchor` 是模块根部连接点。
- `Buds/Tip`、`SideA`、`SideB` 是芽点。位置和旋转分别决定连接位置及初始生长方向。
- `BirdAnchors/Perch` 是小鸟停留点，正式贴图需在此处画出可见枝条。
- `PestAirAnchor` 是该模块空中害虫目标。
- `OmenAnchor` 是预兆中心，运行时在此处沿父芽至尾芽的线段做小幅随机偏移。

镜像和旋转自动带动所有子锚点。成对图片必须同画布、同轮廓、同根部、同芽点位置。未设置图片时可直接看到占位枝条并调整锚点。

芽点系统已经取代旧的完整树形组合以及固定向上、向左、向右贴纸分类，不需要制作九张整树图片。

## 可调参数

在检查器中打开 `config/mvp.tres`，全部临时数值都在这个配置资源里。脚本提供默认值，保存更改会写入该资源。

| 内容 | 当前临时值 |
|---|---|
| 天数 / 爱心 | 6 天 / 3 颗 |
| 白天 / 黄昏 / 夜晚 | 40 秒 / 10 秒 / 30 秒 |
| 每天资源 | 阳光 6、水滴 6，不跨日保留 |
| 枝条价格 | 单芽各 1，双芽各 2，三芽各 3 |
| 垂直层高 | 100 像素，从初始节点顶部到新枝尾芽的垂直距离计算，向上取整 |
| 橡果 / 羽毛 | 第 1～3 层每天合计保底 1、最多 3 个橡果，保底后概率 30%；第 4 层起羽毛概率 100% |
| 旋转 | 芽点方向基础上 ±30°，每步 15°，暂不允许向下 |
| 试作上限 | 30 根枝条、连接深度 8、每类伙伴最多 3 只 |
| 首夜 | 无虫，留出首次生长和伙伴到场的教学时间 |

层数依据世界垂直距离，连接深度是独立上限；横向接得多不会自动变成高层。概率、层高、价格、旋转与上限均是可替换的开发默认值。

白天、黄昏、夜晚现在按 40/10/30 秒固定计时；伙伴提前到齐或害虫提前清空不会提前结束阶段。夜晚到时清空剩余虫，最后一夜到时结算，爱心归零仍立即失败。绿叶指针顺时针经过黄色半圈、橙色八分之一圈、蓝色八分之三圈；翻页期间停住。

## 验证

`tests/mvp_suite.gd` 提供生长连接、跨日开放、垂直层级、预兆延迟、三鸟身份、驱虫、六天循环与胜负的验证。测试通过游戏内执行 `load("res://tests/mvp_suite.gd").new().run(get_tree())` 调用。

树木和天空已接入本轮提供的素材，原图保存在 arsset/nature。贴纸态暂用成长图加运行时纸边，翻页后去掉纸边。虫害美术、逐帧动作和音效仍待后续补充。天空大小、构图中心和黄昏偏移均可在 config/mvp.tres 调整。


底部现在只有两个木制托盘：左侧资源，右侧三种枝条。位置可拖动 UI/ResourcePocketAnchor 和 UI/GrowthTrayAnchor；右侧可拖动贴纸位置为 UI/TraySlots。Overlay 的 Resource Tray Size、Growth Tray Size、Clock Size 和 Leaf Pivot Uv 可调大小及指针转轴。
