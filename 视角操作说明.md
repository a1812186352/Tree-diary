# 视角控制与扩展世界

滚轮以鼠标指向的位置为中心缩放，范围为45%至150%；按住鼠标中键拖动，双击中键回到初始视角。暂停菜单和翻页期间停止视角操作。

相机逻辑独立在 scripts/world_camera.gd。树、枝条、土地使用世界坐标，UI使用固定屏幕坐标；伙伴、收集物和芽点的绘制经过同一相机转换。鼠标位置反向换算回世界坐标，因此成长连接、碰撞、吸附距离继续使用原有数值。

生长范围在 config/mvp.tres 的 growth_bounds 中扩展到横向4800、纵向3155个世界单位，顶部可到-2400。原有枝条数量和连接层数限制仍然有效。

天空及绘本底色保持铺屏。第二步已接入土地延展、可见区域资源生成和随树木定位的入口，具体见下文。大幅拖远时可以双击中键返回树根；当天资源保持生成时的世界位置，不随复位搬移。

请用户体验缩放后收集、拖动视角后放置枝条，以及原画面顶端之外继续生长。按协作约定，本次不主动运行游戏验收。

## 第二步：背景延展、资源和入口

- 土地与远丘复用 `arsset/nature/ground_grass.png`、`hill_arc.png`，树根附近保持原有贴图比例和基准位置；左右镜像接续，底部反复接续土壤区域，上缘柔和融入现有天空。没有制作或修改图片文件。
- `World/Landscape` 由 `scripts/landscape.gd` 与 `shaders/land_extend.gdshader` 绘制当前视野，避免扩大图片尺寸；昼夜土地色调继续沿用现有过渡。
- 地表高度与左右镜像重复同步。仓鼠、访客和地面虫走延展后的地表；原中央地表控制点继续保留。
- 每天开始时，资源按原标记点的画面比例映射到当前视野，并限制在避开顶部菜单、时钟和底部托盘的区域。仍为默认阳光 6、水滴 6，收集距离、支付和跨日清空规则沿用现有配置。
- 资源生成后固定在世界中。当天拖远可能看不到它们，需要回到生成位置；镜头移动不刷新、不补发资源。
- 地面伙伴及地面虫从树根两侧进入，出生高度取实际地表。空中伙伴、访客及飞虫从最高成熟枝条目标两侧进入，无成熟新枝时使用原空中目标。
- 入口不跟随镜头，距离受配置控制，避免缩放让进攻距离无限变长。已经生成的动物和虫继续原有路线，不因拖动镜头瞬移。
- `config/mvp.tres` 的脚本默认参数“扩展世界入口”：伙伴水平距离 890、害虫水平距离 1060、空中入口高于目标 365，单位均为世界坐标。运行入口取代原 Entry 标记点，旧标记点保留供参考。

## 本轮请用户试玩

1. 缩到 45%，左右及向下拖动：地面是否还露出矩形边缘，左右接续、土壤重复与上缘过渡是否自然。
2. 双击中键复位：树根构图、固定托盘和时钟是否正常。
3. 在不同缩放与拖动位置等待新一天：阳光、水滴是否出现在当前视野且可收集；随后拖动，确认资源保持世界位置。
4. 白天、黄昏、夜晚观察延展地面色调，以及仓鼠和地面虫是否沿草地行走。
5. 树向上、向侧面长高后，观察鸟与飞虫入口、到达时间和攻击行为；确认原枝条放置、跨日成熟与翻页仍符合预期。

按用户要求，本轮未运行游戏、自动化测试或收尾自查；以上为待用户验证内容。
