# 两张结局插画

当前布局已统一为绘本阅读跨页：左页完整插画，右页标题、正文和“再种一棵”。尺寸与故事页一致，详见 `绘本布局说明.md`；下文旧版“插画下方文字”布局已由跨页布局替代。

- 存活成功：沿用撑过第六夜且仍有爱心的判定，显示 `assets/endings/ending_survival.png`，对应用户提供的繁茂树木与动物图。
- 昆虫侵占：沿用爱心归零即失败的判定，显示 `assets/endings/ending_invasion.png`，对应用户提供的虫害树木图。
- 图片原样复制，保持完整比例；暖米白纸色铺屏。插画下方独立放置结局文字和“再种一棵”按钮，不遮挡图片。
- 结局显示在固定界面层，隐藏资源、托盘、时钟和顶部菜单，停止镜头操作。重开沿用原逻辑，直接开始第一天。
- 配置位置：`config/mvp.tres` 的“结局插画”，可以替换成功和失败图片。胜负音乐继续沿用已有 MusicManager 配置。
- 本次未运行游戏或测试。请用户分别体验成功、失败能否显示正确图片；结束前缩放或拖动镜头后，结局是否完整；“再种一棵”是否能正常重新开始。
